=render "admin/nav"
.body
  %p In this section you can define the text you see on the home and about pages.
  %h3 Home
  .index
    -@home_mini_posts.each do |m|
      .card
        =link_to edit_admin_mini_post_path(m), style: "color: white;" do
          .content
            .title{style: "color: #5c5c5c"}
              %strong=m.title
              %p=m.body
  %h3 Contact
  .index
    .card
      =link_to edit_admin_mini_post_path(@contact_mini_post), style: "color: white;" do
        .content
          .title{style: "color: #5c5c5c"}
            %strong=@contact_mini_post.title
            %p=@contact_mini_post.body

